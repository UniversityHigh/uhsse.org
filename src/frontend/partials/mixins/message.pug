mixin message(name, description)
	.message(class!=attributes.class)
		if name
			+message-header(name)
		+message-body(description)
			block

mixin message-header(name)
	b.message-header= name

mixin message-body(description)
	p.message-body
		=description